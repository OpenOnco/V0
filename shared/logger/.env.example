# Betterstack Configuration
# Get these from: Betterstack Dashboard > Logs > Sources > Your Source
BETTERSTACK_TOKEN=your_source_token_here
BETTERSTACK_ENDPOINT=https://s123456.region.betterstackdata.com

# Transport mode: console, betterstack, or both
# - console: local output only (default in dev)
# - betterstack: send to Betterstack only (default in prod)
# - both: local output AND Betterstack
LOG_TRANSPORT=console

# Log level: trace, debug, info, warn, error, fatal, silent
# Default: debug (dev), info (prod), silent (test)
LOG_LEVEL=debug

# Pretty print (true for colorized output, false for JSON)
# Default: true (dev), false (prod)
LOG_PRETTY=true

# File logging (Node.js only)
# Creates rotating daily logs with 14-day retention
LOG_TO_FILE=false
LOG_DIR=./logs
