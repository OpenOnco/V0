#!/bin/bash
# OpenOnco Preview: smoke tests → push to develop → Vercel preview URL

set -e
cd "$(dirname "$0")"

echo "🧪 Running smoke tests..."
npm run test:smoke

echo ""
echo "✅ Tests passed. Pushing to develop..."
git add -A
git commit -m "${1:-wip: preview}" --allow-empty
git push

echo ""
echo "⏳ Waiting for Vercel deployment..."
sleep 15

# Get commit hash for unique URL
COMMIT_HASH=$(git rev-parse --short HEAD)

echo ""
echo "🚀 Preview deployed!"
echo ""
echo "📎 https://v0-42kj-git-develop-alex-dickinsons-projects-2bee58ff.vercel.app"
echo ""
