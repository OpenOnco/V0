#!/bin/bash
# OpenOnco Release: full test suite → merge develop into main → openonco.org

set -e
cd "$(dirname "$0")"

echo "🧪 Running full test suite..."
npm run test:full

echo ""
echo "✅ Tests passed. Pushing to production..."
git add -A
git commit -m "${1:-release}" --allow-empty

git checkout main
git pull origin main --ff-only
git merge develop --no-edit
git push origin main
git checkout develop

echo ""
echo "🚀 Released to openonco.org!"
